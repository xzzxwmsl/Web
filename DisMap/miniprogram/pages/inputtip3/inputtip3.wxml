<view class="section">
  <input bindinput="bindInput" placeholder="搜索" focus="true" />
</view>
<view bindtouchstart="search" data-keywords="{{item.name}}" class="text_box" wx:for="{{tips}}" wx:key="key">
  {{item.name}}
</view>